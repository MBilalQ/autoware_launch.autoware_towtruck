sensor_kit_base_link:
  livox_mid360_base_link:
    x: 0.0
    y: 0.0
    # Mid-360 is mounted inverted; this offset compensates the helper macro's
    # internal scan-frame shift so the published livox_mid360 frame stays at
    # the measured sensor center.
    z: 0.0377
    roll: 3.141592653589793
    pitch: 0.0
    yaw: 0.0
